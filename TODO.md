# TODO: Fix Vercel Build Issue by Moving Vite to Dependencies

- [ ] Edit frontend/package.json to move "vite" and "@vitejs/plugin-react" from devDependencies to dependencies
- [ ] Clean install in frontend directory: remove node_modules and package-lock.json, then run npm install
- [ ] Commit changes with message "Fix Vite build on Vercel"
- [ ] Push changes to GitHub
